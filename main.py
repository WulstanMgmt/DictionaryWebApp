# red is not added to git
# green is added to staging area, which are files and filepaths ready to commit
# blue means there's uncommitted changes in file
# commit saves changes to main file

import pandas as pd
import numpy as np

pd.Series()